package com.appdev.interviewschedulermanagement.config;

// CORS is now fully managed by SecurityConfiguration.corsConfigurationSource()
// which is the correct integration point for Spring Security's filter chain.
// This file is intentionally left empty to avoid duplicate CORS configuration conflicts.